apiVersion: v1
kind: PersistentVolume
metadata:
  name: locatic-sqlite-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /data/locatic